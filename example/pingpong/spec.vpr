define pansel4_has_cap(x) (x in Set(1,2,3,4,15))
define pansel4_has_endpoint_cap(x) (x in Set(2))
define pansel4_has_notification_cap(x) (x in Set(1,15))
define pansel4_has_reply_cap(x) (x in Set(4))
define pansel4_has_page_table_cap(x) (x in Set(3))
define pansel4_has_irq_handler_cap(x) (false)
define pansel4_has_tcb_cap(x) (false)
define pansel4_has_vcpu_cap(x) (false)
define pansel4_has_ioport_cap(x) (false)
define pansel4_has_arm_smc_cap(x) (false)

define f_pansel4_has_cap(heap,gv,x) (x in Set(1,2,3,4,15))
define f_pansel4_has_endpoint_cap(heap,gv,x) (x in Set(2))
define f_pansel4_has_notification_cap(heap,gv,x) (x in Set(1,15))
define f_pansel4_has_reply_cap(heap,gv,x) (x in Set(4))
define f_pansel4_has_page_table_cap(heap,gv,x) (x in Set(3))
define f_pansel4_has_irq_handler_cap(heap,gv,x) (false)
define f_pansel4_has_tcb_cap(heap,gv,x) (false)
define f_pansel4_has_vcpu_cap(heap,gv,x) (false)
define f_pansel4_has_ioport_cap(heap,gv,x) (false)
define f_pansel4_has_arm_smc_cap(heap,gv,x) (false)

define mk_is_protected_source(x) (false)
define mk_is_ppcall_target(x) (false)
define mk_is_notified_source(x) (x in Set(5))
define mk_is_notify_target(x) (x in Set(5))
define mk_is_irq_channel(x) (false)
define mk_is_child(x) (false)

define f_mk_is_protected_source(heap,gv,x) (false)
define f_mk_is_ppcall_target(heap,gv,x) (false)
define f_mk_is_notified_source(heap,gv,x) (x in Set(5))
define f_mk_is_notify_target(heap,gv,x) (x in Set(5))
define f_mk_is_irq_channel(heap,gv,x) (false)
define f_mk_is_child(heap,gv,x) (false)
